Criar swap

sudo fallocate -l 1G /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile
sudo swapon --show
sudo chmod 600 /swapfile

sudo nano /etc/fstab

/swapfile   none   swap  sw  0  0

free -m

Crédito - https://medium.com/@leandroembu/criando-um-swapfile-no-linux-838296fbcf3



sudo nano /etc/php/7.2/apache2/php.ini

display_error = On
error_reporting = E_ALL # comentar o existente
output_buffering = On # Default é 4096

sudo nano /etc/apache2/apache2.conf

ServerName localhost

Mudar as ocorrenciasn de 
AllowOverride none
Para
AllowOverride All

sudo service apache2 restart

== Configurar mysql para resetar senha root. Mas antes testar se já funciona normalmente
(Criar script para isso)

sudo mysql -uroot
USE mysql;
UPDATE user SET plugin='mysql_native_password' WHERE User='root';
UPDATE user SET authentication_string=PASSWORD("root") WHERE User='root';
FLUSH PRIVILEGES;
exit;

sudo service mysql restart
mysql -uroot -proot

OPCIONAIS
